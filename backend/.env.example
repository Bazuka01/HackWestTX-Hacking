# Copy this file to backend/.env and fill in both values.
# backend/.env is gitignored and must never be committed.
GEMINI_API_KEY=
TIGER_DATA_URL=postgresql://user:password@host:port/dbname?sslmode=require
